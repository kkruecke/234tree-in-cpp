Tailor the insert test cases for we test split a 3-node whose parent is a 2-node, but ensure the test cases insert the middle  value into the parent both when it is larger than the parent key as well as when it is small.
Then test split a 3-node whose parent is a 3-node, but ensure the test cases insert the middle  value into the parent both when it can vary in size, in all possible ways, with the keys of the parent node.
o
remove() needs to be tested more. Confirm that the output from the tree is a valid 2 3 4 tree.
 
